import Foundation

enum AppSizing {
    enum Spacing: CGFloat, LepsySizing {
        case small = 5
        case medium = 15
    }
    
    enum CornerRadius: CGFloat, LepsySizing {
        case small = 5
    }
}
